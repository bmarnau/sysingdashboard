name: Temporary BSF-03A Task5 Adapter Prettier

on:
  push:
    branches:
      - tmp/bsf03a-task5-adapter-prettier

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tmp/bsf03a-task5-adapter-prettier
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Format only Task 5 adapter
        run: bunx prettier --write src/integrations/supabase/project-controlling-adapter.ts
      - name: Verify only adapter changed
        shell: bash
        run: |
          set -euo pipefail
          changed="$(git status --porcelain | awk '{print $2}')"
          test "$changed" = "src/integrations/supabase/project-controlling-adapter.ts"
      - name: Commit formatted adapter
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/integrations/supabase/project-controlling-adapter.ts
          git commit -m 'chore(tmp): format BSF-03A Task 5 adapter'
          git push origin HEAD:tmp/bsf03a-task5-adapter-prettier
