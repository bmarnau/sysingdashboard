name: Temporary BSF-03A Task6 Test Prettier

on:
  push:
    branches:
      - tmp/bsf03a-task6-test-prettier

permissions:
  contents: write

jobs:
  format:
    runs-on: ubuntu-24.04
    steps:
      - uses: actions/checkout@v4
        with:
          ref: tmp/bsf03a-task6-test-prettier
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest
      - name: Install dependencies
        run: bun install --frozen-lockfile
      - name: Format only Task 6 RED test
        run: bunx prettier --write src/__tests__/backend/project-controlling.functions.test.ts
      - name: Verify only Task 6 test changed
        shell: bash
        run: |
          set -euo pipefail
          changed="$(git status --porcelain | awk '{print $2}')"
          test "$changed" = "src/__tests__/backend/project-controlling.functions.test.ts"
      - name: Commit formatted test
        shell: bash
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/__tests__/backend/project-controlling.functions.test.ts
          git commit -m 'chore(tmp): format BSF-03A Task 6 RED test'
          git push origin HEAD:tmp/bsf03a-task6-test-prettier
